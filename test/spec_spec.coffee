specrunner = require('..')
chai = require('chai')
chai.should()
expect = chai.expect
{Spec} = specrunner

describe 'Spec', ->
  describe 'constructor()', ->
  describe 'describe()', ->
  describe 'it()', ->
  describe 'beforeEach()', ->
  describe 'afterEach()', ->
  describe 'run', ->